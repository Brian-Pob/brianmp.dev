---
let theme = "system";
if (Astro.cookies.has("theme")) {
  const cookie = Astro.cookies.get("theme");
  theme = cookie.value;
}
---
<!doctype html>
<html lang="en" data-theme={theme}>
  <slot />
</html>
